# frozen_string_literal: true

RSpec.describe Menubot do
  it "has a version number" do
    expect(Menubot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
